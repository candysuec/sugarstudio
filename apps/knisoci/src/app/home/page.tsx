import { Button } from '@sugarstudio/ui'

export default function Home() {
  return (
    <main>
      <h1>🧠 KniSoci is running</h1>
      <Button onClick={() => {}}>Click Me</Button>
    </main>
  )
}